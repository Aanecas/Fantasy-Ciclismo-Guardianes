"use client";

// Componente neutral: no muestra nada ni hace llamadas.
// Lo dejamos así porque el alta en Sheets ya se hace automáticamente
// en el callback de sign-in.
export default function CompleteSignupButton() {
  return null;
}
